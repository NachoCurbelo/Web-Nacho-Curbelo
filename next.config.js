module.exports = {
  output: 'export',
  exportTrailingSlash: true, // Optional, to add trailing slashes to URLs
  images: {
    unoptimized: true, // Optional, depends on your usage of images
  },
};
